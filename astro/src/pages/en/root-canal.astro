---
/**
 * /en/root-canal — porte fiel de src/pages/en/EnRootCanalPage.tsx (React SPA).
 * Copy VERBATIM. Layout: SiteLayout lang="en" (EnSiteHeader/EnSiteFooter).
 * Tracking: CTAs WhatsApp via classe global js-wa-cta (handler no Base.astro).
 * Nota: canal é executado por endodontista parceiro — a copy do React já reflete isso.
 */
import SiteLayout from '../../layouts/SiteLayout.astro';

// FAQs — verbatim do React (usadas no HTML e no schema FAQPage)
const faqs = [
  {
    question: "Does root canal treatment hurt?",
    answer: "With modern anesthesia, the procedure is comfortable and well-tolerated. What usually causes pain is the infection that led to the need for the root canal. The treatment actually relieves that pain. After the procedure, there may be mild sensitivity for a few days, easily managed with simple medication."
  },
  {
    question: "How many sessions are needed?",
    answer: "In most cases, 1 to 2 sessions. It depends on the case complexity — teeth with more canals or more extensive infections may require additional sessions. The endodontist evaluates and informs you before starting."
  },
  {
    question: "Does the tooth become fragile after a root canal?",
    answer: "The tooth loses its internal nutrition, which can make it more susceptible to fractures over time. That's why proper restoration is essential. Depending on the case, Dr. Carla may recommend a crown to protect the tooth long-term."
  },
  {
    question: "Does the tooth darken after a root canal?",
    answer: "It can happen over time, but it's not a rule. When it occurs, there are options to resolve it — from internal whitening to a veneer or crown. Dr. Carla evaluates the best solution during follow-up."
  },
  {
    question: "What's the alternative to root canal treatment?",
    answer: "The alternative would be tooth extraction. But whenever possible, preserving the natural tooth is the best choice — it avoids the need for an implant or prosthesis and maintains the original structure of the mouth."
  },
  {
    question: "Do you accept dental insurance?",
    answer: "Our care is private (fee-for-service), which allows us to dedicate the necessary time to each case and work with selected partner professionals. During the consultation, we present the complete treatment plan."
  }
];

// Schemas — copiados VERBATIM do SEOHead da página React
const schemas = [
  JSON.stringify({ "@context": "https://schema.org", "@type": "MedicalProcedure", "name": "Root Canal Treatment (Endodontics)", "description": "Endodontic treatment with comprehensive follow-up and restorative finalization", "procedureType": "Dental", "inLanguage": "en", "provider": { "@type": "Dentist", "name": "Dr. Carla Christoph", "telephone": "+5521993304045", "address": { "@type": "PostalAddress", "streetAddress": "Rua Visconde de Pirajá, 550 - Suite 1107", "addressLocality": "Ipanema", "addressRegion": "RJ", "postalCode": "22410-901", "addressCountry": "BR" } } }),
  JSON.stringify({ "@context": "https://schema.org", "@type": "FAQPage", "inLanguage": "en", "mainEntity": faqs.map(faq => ({ "@type": "Question", "name": faq.question, "acceptedAnswer": { "@type": "Answer", "text": faq.answer } })) }),
  JSON.stringify({ "@context": "https://schema.org", "@type": "WebPage", "name": "Root Canal Treatment in Ipanema", "url": "https://dracarlachristoph.com/en/root-canal", "speakable": { "@type": "SpeakableSpecification", "cssSelector": [".treatment-hero-title", ".treatment-hero-description", ".quick-answer-box"] }, "inLanguage": "en" }),
  // BreadcrumbList — o TreatmentHero do React injeta este schema via Helmet
  JSON.stringify({
    "@context": "https://schema.org", "@type": "BreadcrumbList",
    "itemListElement": [
      { "@type": "ListItem", "position": 1, "name": "Home", "item": "https://dracarlachristoph.com/en" },
      { "@type": "ListItem", "position": 2, "name": "Treatments", "item": "https://dracarlachristoph.com/en" },
      { "@type": "ListItem", "position": 3, "name": "Root Canal" }
    ]
  }),
];

// WhatsApp CTAs — mensagens exatas do React
const WA = 'https://wa.me/5521993304045?text=';
const waHero = WA + encodeURIComponent('Hello! I would like to schedule a consultation about Root Canal Treatment in Ipanema.');
// Urgent CTA: href literal do React (já URL-encoded na origem)
const waUrgent = "https://wa.me/5521993304045?text=Hello!%20I'm%20experiencing%20symptoms%20that%20may%20indicate%20the%20need%20for%20root%20canal%20treatment.%20I'd%20like%20to%20schedule%20an%20urgent%20evaluation.";
const waFinal = WA + encodeURIComponent('Hello! I saw the page about root canal treatment and would like to schedule an evaluation with Dr. Carla Christoph.');

// Ícones lucide (inline SVG, stroke currentColor) — paridade visual com o React
const P: Record<string, string> = {
  calendar: '<rect width="18" height="18" x="3" y="4" rx="2" ry="2"/><line x1="16" x2="16" y1="2" y2="6"/><line x1="8" x2="8" y1="2" y2="6"/><line x1="3" x2="21" y1="10" y2="10"/>',
  shield: '<path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>',
  alert: '<circle cx="12" cy="12" r="10"/><line x1="12" x2="12" y1="8" y2="12"/><line x1="12" x2="12.01" y1="16" y2="16"/>',
  heart: '<path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/>',
  sparkles: '<path d="M12 3l1.912 5.813a2 2 0 0 0 1.275 1.275L21 12l-5.813 1.912a2 2 0 0 0-1.275 1.275L12 21l-1.912-5.813a2 2 0 0 0-1.275-1.275L3 12l5.813-1.912a2 2 0 0 0 1.275-1.275L12 3z"/>',
  award: '<circle cx="12" cy="8" r="7"/><polyline points="8.21 13.89 7 23 12 20 17 23 15.79 13.88"/>',
  help: '<circle cx="12" cy="12" r="10"/><path d="M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3"/><line x1="12" x2="12.01" y1="17" y2="17"/>',
  check: '<path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/>',
  check2: '<circle cx="12" cy="12" r="10"/><path d="m9 12 2 2 4-4"/>',
  arrow: '<path d="M5 12h14"/><path d="m12 5 7 7-7 7"/>',
  users: '<path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M22 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/>',
  star: '<polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/>',
  search: '<circle cx="11" cy="11" r="8"/><path d="m21 21-4.3-4.3"/>',
  zap: '<polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/>',
  chevdown: '<path d="m6 9 6 6 6-6"/>',
  chevright: '<path d="m9 18 6-6-6-6"/>',
};
const icon = (name: string, size = 24, fill = false) =>
  `<svg xmlns="http://www.w3.org/2000/svg" width="${size}" height="${size}" viewBox="0 0 24 24" fill="${fill ? 'currentColor' : 'none'}" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">${P[name]}</svg>`;

// Process Timeline — verbatim (nota: "Duração:" é hardcoded no componente React mesmo em EN)
const timelineSteps = [
  { number: 1, title: 'Diagnosis with Dr. Carla', description: 'Clinical exam, X-ray, and case evaluation. Dr. Carla identifies the problem and explains what needs to be done.', iconName: 'search', duration: '1st Appointment' },
  { number: 2, title: 'Treatment with Endodontist', description: 'The procedure is performed by a specialized endodontist with local anesthesia. Dr. Carla personally oversees the process.', iconName: 'shield', duration: '1-2 sessions' },
  { number: 3, title: 'Restoration by Dr. Carla', description: 'After the root canal, Dr. Carla finalizes the tooth — with a direct composite restoration, crown, or the most appropriate solution for your case.', iconName: 'sparkles', duration: 'Finalization' },
  { number: 4, title: 'Follow-Up', description: 'Follow-up appointments to ensure the treated tooth is healthy and the restoration is intact.', iconName: 'heart', duration: 'Periodic' },
];

// Related Treatments (InternalLinkingOptimizer → 'en-root-canal')
const relatedLinks = [
  { title: 'Aesthetic Restorations', url: '/en/aesthetic-restorations', description: 'Tooth reconstruction after root canal' },
  { title: 'Dental Prosthetics', url: '/en/dental-prosthetics', description: 'Crown for post-treatment protection' },
  { title: 'Dental Implants', url: '/en/dental-implants', description: 'Alternative if extraction is needed' },
];
---

<SiteLayout
  title="Root Canal Treatment in Ipanema | Dr. Carla Christoph, Rio de Janeiro"
  description="Pain-free root canal treatment in Ipanema with modern techniques. Save your natural tooth with expert endodontic care. Dr. Carla Christoph, CRO-RJ 27.509."
  canonical="https://dracarlachristoph.com/en/root-canal"
  preloadImage="/lovable-uploads/dra-carla-jaleco-bracos-cruzados.webp"
  schemas={schemas}
  lang="en"
>
  <Fragment slot="head">
    <link rel="alternate" hreflang="pt-BR" href="https://dracarlachristoph.com/tratamento-de-canal" />
    <link rel="alternate" hreflang="en" href="https://dracarlachristoph.com/en/root-canal" />
    <link rel="alternate" hreflang="x-default" href="https://dracarlachristoph.com/tratamento-de-canal" />
  </Fragment>

  <main id="main-content" class="pg-rootcanal">
    <!-- 1. Hero Section -->
    <section class="t-hero" aria-label="Treatment hero">
      <div class="t-hero-inner">
        <div class="t-hero-grid">
          <div class="t-hero-content">
            <nav class="t-bc" aria-label="Breadcrumb">
              <a href="/en">Home</a>
              <span class="t-bc-sep" set:html={icon('chevright', 14)} />
              <a href="/en">Treatments</a>
              <span class="t-bc-sep" set:html={icon('chevright', 14)} />
              <span class="t-bc-cur">Root Canal</span>
            </nav>
            <h1 class="treatment-hero-title">Root Canal Treatment in Ipanema</h1>
            <p class="t-hero-sub">Comprehensive Care with Dr. Carla Christoph</p>
            <p class="treatment-hero-description">A procedure with an unfair reputation. With experienced professionals and individualized follow-up, root canal treatment is more comfortable than you might think.</p>
            <div class="t-badges">
              <span class="t-badge">Comprehensive Follow-Up</span>
              <span class="t-badge">Specialized Endodontist</span>
              <span class="t-badge">CRO-RJ 27.509</span>
            </div>
            <div class="t-hero-cta">
              <a
                class="btn-hero js-wa-cta"
                data-label="en_root_canal_hero_whatsapp"
                href={waHero}
                target="_blank"
                rel="noopener"
                aria-label="Schedule consultation via WhatsApp"
              >
                <span class="i" set:html={icon('calendar', 20)} />
                Book Your Consultation
              </a>
            </div>
          </div>
          <div class="t-hero-imgcol">
            <div class="t-hero-imgframe">
              <img
                src="/lovable-uploads/dra-carla-jaleco-bracos-cruzados.webp"
                alt="Dr. Carla Christoph - Root Canal Treatment in Ipanema Specialist"
                width="800"
                height="1200"
                loading="eager"
                fetchpriority="high"
              />
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Quick Answer -->
    <section class="qa-section">
      <div class="cc">
        <div class="quick-answer-box">
          <span class="qa-icon" set:html={icon('check2', 24)} />
          <div>
            <h2>Quick Answer</h2>
            <p>Root canal treatment (endodontics) removes the infected pulp from the tooth, relieving pain and saving the tooth from extraction. At Dr. Carla Christoph's office in Ipanema (CRO-RJ 27.509), treatment is performed by a specialized endodontist and finalized by Dr. Carla. The procedure is comfortable with modern anesthesia, usually completed in 1-2 sessions. After the root canal, proper restoration or a crown is recommended to protect the tooth, which can last many years with proper care.</p>
          </div>
        </div>
      </div>
    </section>

    <div class="divider"><span class="d-line"></span><span class="d-circle" set:html={icon('shield', 20)} /><span class="d-line"></span></div>

    <!-- Empathetic Pain Points -->
    <section class="pp-section">
      <div class="mw-4xl">
        <h2 class="pp-title">Root canal treatment doesn't have to be scary</h2>
        <div class="pp-list">
          <div class="pp-item b-purple">
            <span class="pp-icon c-purple" set:html={icon('shield', 24)} />
            <div>
              <strong>Fear of pain during the procedure</strong>
              <span>With modern anesthesia and updated techniques, the procedure is comfortable. What hurts is the infection — the root canal actually relieves that pain.</span>
            </div>
          </div>
          <div class="pp-item b-gold">
            <span class="pp-icon c-gold" set:html={icon('alert', 24)} />
            <div>
              <strong>Putting it off for months due to anxiety</strong>
              <span>Delaying can turn simple treatment into something more complex. The sooner it's diagnosed, the better the chance of saving the tooth.</span>
            </div>
          </div>
          <div class="pp-item b-purple-soft">
            <span class="pp-icon c-purple-soft" set:html={icon('heart', 24)} />
            <div>
              <strong>Need comprehensive care</strong>
              <span>From diagnosis to final restoration, Dr. Carla coordinates the entire journey with a specialized endodontist.</span>
            </div>
          </div>
        </div>
        <p class="pp-conclusion">Modern root canal treatment is much more comfortable than you might imagine.<br class="md-only" /> Take the first step: schedule your evaluation.</p>
      </div>
    </section>

    <div class="divider"><span class="d-line"></span><span class="d-circle" set:html={icon('alert', 20)} /><span class="d-line"></span></div>

    <!-- Warning Signs — 3 Cards -->
    <section class="ws-section">
      <div class="cc">
        <h2 class="sec-title">When Is Root Canal Treatment Necessary?</h2>
        <p class="sec-sub">Warning signs that deserve attention</p>
        <div class="ws-grid mw-5xl">
          <div class="ws-card">
            <span class="ws-icon bg-purple c-purple" set:html={icon('zap', 32)} />
            <h3>Persistent Pain</h3>
            <p>Spontaneous, throbbing toothache or pain that worsens with heat. Prolonged sensitivity that doesn't go away. These are signs that the tooth's pulp may be compromised.</p>
          </div>
          <div class="ws-card">
            <span class="ws-icon bg-gold c-gold" set:html={icon('alert', 32)} />
            <h3>Swelling or Abscess</h3>
            <p>Swollen gum near a tooth, presence of pus, or a fistula. Indicates infection that needs treatment to prevent spreading.</p>
          </div>
          <div class="ws-card">
            <span class="ws-icon bg-purple c-purple" set:html={icon('sparkles', 32)} />
            <h3>Darkened Tooth</h3>
            <p>A tooth that has darkened after trauma or deep cavity may have a compromised pulp. The root canal treats the cause and allows restoring the tooth's appearance.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Urgent CTA -->
    <section class="urgent-cta">
      <div class="mw-3xl">
        <p>Experiencing any of these symptoms?</p>
        <a
          class="btn-urgent js-wa-cta"
          data-label="en_root_canal_urgent_whatsapp"
          href={waUrgent}
          target="_blank"
          rel="noopener"
        >
          Schedule Urgent Evaluation
          <span class="i" set:html={icon('arrow', 20)} />
        </a>
      </div>
    </section>

    <!-- Doctor Bio -->
    <section class="bio-section">
      <div class="cc">
        <h2 class="bio-title">Why Choose Dr. Carla Christoph</h2>
        <div class="mw-5xl">
          <div class="bio-grid">
            <div class="bio-photo">
              <div class="bio-photo-frame">
                <img
                  src="/lovable-uploads/dra-carla-jaleco-bracos-cruzados.webp"
                  alt="Dr. Carla Christoph - Dental Specialist"
                  width="800"
                  height="1200"
                  loading="lazy"
                  decoding="async"
                />
                <div class="bio-badge">Complete Journey</div>
              </div>
            </div>
            <div class="bio-card">
              <div class="bio-card-head">
                <h3>Dr. Carla Christoph</h3>
                <p>CRO-RJ 27.509 | Specialist in Prosthodontics and Implant Dentistry</p>
              </div>
              <div class="bio-paragraphs">
                <p>With over two decades in Ipanema, Dr. Carla has built her reputation by treating each patient individually, with time and attention. Her background includes 8 years as a military dentist at the Brazilian Navy Dental Center, an experience that brought discipline and precision to her clinical practice.</p>
                <p>For root canal treatment, Dr. Carla personally oversees each stage and is responsible for the finalization: direct restoration, crown, or prosthesis. You have one professional caring for your entire journey — from diagnosis to the final result.</p>
              </div>
              <div class="bio-creds">
                <div class="bio-cred">
                  <span class="bio-cred-icon" set:html={icon('check', 20)} />
                  <div>
                    <p class="bio-cred-title">Coordination</p>
                    <p class="bio-cred-desc">Personal oversight of the entire treatment journey</p>
                  </div>
                </div>
                <div class="bio-cred">
                  <span class="bio-cred-icon" set:html={icon('check', 20)} />
                  <div>
                    <p class="bio-cred-title">Finalization</p>
                    <p class="bio-cred-desc">Restoration, crown, or prosthesis done by Dr. Carla</p>
                  </div>
                </div>
                <div class="bio-cred">
                  <span class="bio-cred-icon" set:html={icon('check', 20)} />
                  <div>
                    <p class="bio-cred-title">Experience</p>
                    <p class="bio-cred-desc">20+ years, including 8 in the Navy</p>
                  </div>
                </div>
                <div class="bio-cred">
                  <span class="bio-cred-icon" set:html={icon('check', 20)} />
                  <div>
                    <p class="bio-cred-title">Approach</p>
                    <p class="bio-cred-desc">Individualized appointments, minimum 1 hour</p>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <div class="divider"><span class="d-line"></span><span class="d-circle" set:html={icon('search', 20)} /><span class="d-line"></span></div>

    <!-- Process Timeline -->
    <section class="tl-section">
      <div class="cc">
        <h2 class="sec-title tl-title">How Root Canal Treatment Works at Our Office</h2>
        <div class="mw-4xl">
          <div class="timeline" role="list">
            <div class="tl-vline" aria-hidden="true"></div>
            <div class="tl-steps">
              {timelineSteps.map((step) => (
                <div class="tl-step" role="listitem">
                  <div class="tl-num-col">
                    <div class="tl-num">{step.number}</div>
                    <div class="tl-dot" aria-hidden="true"></div>
                  </div>
                  <div class="tl-content">
                    <span class="tl-icon" set:html={icon(step.iconName, 24)} />
                    <h3>{step.title}</h3>
                    <p>{step.description}</p>
                    <p class="tl-duration">Duração: {step.duration}</p>
                  </div>
                </div>
              ))}
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Informational Section -->
    <section class="info-section">
      <div class="cc">
        <h2 class="sec-title info-title">What Happens During the Procedure</h2>
        <div class="info-body mw-3xl">
          <p>Root canal treatment involves removing the tooth's pulp — the internal tissue containing nerves and blood vessels — when it becomes inflamed or infected. With the tooth anesthetized, the endodontist accesses the interior, cleans and disinfects the canals, and fills them with biocompatible material. The procedure typically takes 1 to 2 sessions, and the tooth is then restored by Dr. Carla to recover its form, function, and appearance.</p>
          <p>In clinical practice, what we see most are patients who arrive tense and leave relieved — both because the pain subsides and because they discover the procedure was much more comfortable than they imagined.</p>
        </div>
      </div>
    </section>

    <div class="divider"><span class="d-line"></span><span class="d-circle" set:html={icon('help', 20)} /><span class="d-line"></span></div>

    <!-- FAQs -->
    <section class="faq-section">
      <div class="cc">
        <h2 class="faq-title">Questions About Root Canal Treatment</h2>
        <div class="mw-4xl">
          <div class="faq-list">
            {faqs.map((faq) => (
              <details class="faq-item">
                <summary>{faq.question} <span class="chev" set:html={icon('chevdown', 16)} /></summary>
                <div class="faq-a">{faq.answer}</div>
              </details>
            ))}
          </div>
        </div>
      </div>
    </section>

    <!-- Final CTA -->
    <section class="final-cta">
      <div class="cc">
        <div class="final-cta-head">
          <span class="final-cta-icon" set:html={icon('shield', 32)} />
          <h2>Tooth Pain Needs Attention</h2>
        </div>
        <p class="final-cta-desc">Schedule your evaluation. The sooner the diagnosis, the simpler the treatment tends to be.</p>
        <div class="final-cta-btn-wrap">
          <a
            class="btn-final js-wa-cta"
            data-label="en_root_canal_service_page_cta"
            href={waFinal}
            target="_blank"
            rel="noopener"
          >
            <span class="i" set:html={icon('arrow', 20)} />
            Schedule Evaluation
          </a>
        </div>
        <p class="final-cta-footnote">WhatsApp: (21) 99330-4045 | 24h Service</p>
      </div>
    </section>

    <!-- Related Treatments (InternalLinkingOptimizer) -->
    <section class="rel-section">
      <div class="mw-4xl">
        <h2>Related Treatments</h2>
        <div class="rel-grid">
          {relatedLinks.map((link) => (
            <a href={link.url} class="rel-card">
              <span class="rel-title">{link.title}</span>
              <span class="rel-desc">{link.description}</span>
              <span class="rel-more">Learn more <span class="i" set:html={icon('arrow', 12)} /></span>
            </a>
          ))}
        </div>
      </div>
    </section>

    <!-- Stats Bar -->
    <section class="stats-bar" aria-label="Clinic statistics">
      <div class="cc">
        <div class="stats-grid">
          <div class="stat">
            <span class="stat-icon" set:html={icon('calendar', 20)} />
            <div><p class="stat-value">20+</p><p class="stat-label">Years of Experience</p></div>
          </div>
          <div class="stat">
            <span class="stat-icon" set:html={icon('users', 20)} />
            <div><p class="stat-value">10,000+</p><p class="stat-label">Patients Served</p></div>
          </div>
          <div class="stat">
            <span class="stat-icon" set:html={icon('award', 20)} />
            <div><p class="stat-value">CRO-RJ</p><p class="stat-label">27.509</p></div>
          </div>
          <div class="stat">
            <span class="stat-icon" set:html={icon('star', 20, true)} />
            <div><p class="stat-value">5.0 ★</p><p class="stat-label">Google Rating</p></div>
          </div>
        </div>
      </div>
    </section>
  </main>
</SiteLayout>

<style is:global>
  /* ══════════ /en/root-canal — estilos (réplica dos visuais Tailwind do SPA) ══════════ */
  .pg-rootcanal {
    --purple: #381f47;
    --purple-soft: #5a3f69;
    --purple-dark: #2a1735;
    --gold: #b3955f;
    --gold-dark: #9d7f4f;
    --gray: #525252;
    font-family: Montserrat, system-ui, -apple-system, sans-serif;
  }
  .pg-rootcanal .i { display: inline-flex; }
  .pg-rootcanal .i svg { display: block; }

  /* containers */
  .pg-rootcanal .cc { max-width: 1280px; margin: 0 auto; padding-left: 24px; padding-right: 24px; }
  .pg-rootcanal .mw-3xl { max-width: 768px; margin: 0 auto; padding: 0 16px; }
  .pg-rootcanal .mw-4xl { max-width: 896px; margin: 0 auto; padding: 0 16px; }
  .pg-rootcanal .mw-5xl { max-width: 1024px; margin: 0 auto; }
  @media (min-width: 1024px) {
    .pg-rootcanal .cc { padding-left: 32px; padding-right: 32px; }
  }
  .pg-rootcanal .md-only { display: none; }
  @media (min-width: 768px) { .pg-rootcanal .md-only { display: block; } }

  /* títulos de seção */
  .pg-rootcanal .sec-title {
    font-family: 'Playfair Display', Georgia, serif;
    font-weight: 600; font-size: 1.875rem;
    color: var(--purple); text-align: center; margin-bottom: 16px;
  }
  @media (min-width: 768px) { .pg-rootcanal .sec-title { font-size: 2.25rem; } }
  .pg-rootcanal .sec-sub { text-align: center; color: var(--gray); margin-bottom: 48px; max-width: 672px; margin-left: auto; margin-right: auto; }

  /* ── Hero ── */
  .pg-rootcanal .t-hero { background: linear-gradient(135deg, hsl(250 100% 98%) 0%, hsl(30 100% 97%) 100%); }
  .pg-rootcanal .t-hero-inner { max-width: 1280px; margin: 0 auto; padding: 112px 24px 64px; }
  @media (min-width: 768px) { .pg-rootcanal .t-hero-inner { padding-bottom: 80px; } }
  @media (min-width: 1024px) { .pg-rootcanal .t-hero-inner { padding: 120px 32px 96px; } }
  .pg-rootcanal .t-hero-grid { display: grid; gap: 32px; align-items: center; }
  .pg-rootcanal .t-hero-content { display: flex; flex-direction: column; gap: 24px; }
  .pg-rootcanal .t-hero-imgcol { order: -1; }
  @media (min-width: 1024px) {
    .pg-rootcanal .t-hero-grid { grid-template-columns: repeat(5, 1fr); gap: 48px; }
    .pg-rootcanal .t-hero-content { grid-column: span 3; }
    .pg-rootcanal .t-hero-imgcol { grid-column: span 2; order: 2; }
  }
  .pg-rootcanal .t-bc { display: flex; flex-wrap: wrap; align-items: center; gap: 6px; font-size: 0.875rem; opacity: 0.7; }
  .pg-rootcanal .t-bc a { color: var(--purple-soft); text-decoration: none; transition: color 0.2s; }
  .pg-rootcanal .t-bc a:hover { color: var(--purple); }
  .pg-rootcanal .t-bc-sep { color: var(--gray); display: inline-flex; }
  .pg-rootcanal .t-bc-cur { color: var(--purple); font-weight: 500; }
  .pg-rootcanal .treatment-hero-title {
    font-family: 'Playfair Display', Georgia, serif;
    font-weight: 400; font-size: 2.25rem; line-height: 1.25;
    color: var(--purple);
  }
  @media (min-width: 768px) { .pg-rootcanal .treatment-hero-title { font-size: 3rem; } }
  @media (min-width: 1024px) { .pg-rootcanal .treatment-hero-title { font-size: 3.75rem; } }
  .pg-rootcanal .t-hero-sub { font-size: 1.25rem; color: var(--purple-soft); font-weight: 500; }
  @media (min-width: 768px) { .pg-rootcanal .t-hero-sub { font-size: 1.5rem; } }
  .pg-rootcanal .treatment-hero-description { font-size: 1.125rem; color: var(--gray); line-height: 1.625; max-width: 672px; }
  .pg-rootcanal .t-badges { display: flex; flex-wrap: wrap; gap: 12px; }
  .pg-rootcanal .t-badge {
    display: inline-flex; align-items: center;
    background: rgb(179 149 95 / 0.1);
    color: var(--gold-dark);
    border: 1px solid rgb(179 149 95 / 0.2);
    border-radius: 999px;
    padding: 6px 16px;
    font-size: 0.875rem; font-weight: 500;
    transition: background-color 0.2s;
  }
  .pg-rootcanal .t-badge:hover { background: rgb(179 149 95 / 0.15); }
  .pg-rootcanal .t-hero-cta { padding-top: 16px; }
  .pg-rootcanal .btn-hero {
    display: inline-flex; align-items: center; gap: 8px;
    background: var(--purple); color: #fff;
    padding: 20px 24px; border-radius: 6px;
    font-size: 1rem; font-weight: 500; text-decoration: none;
    box-shadow: 0 4px 20px hsl(0 0% 0% / 0.08);
    transition: all 0.3s;
  }
  .pg-rootcanal .btn-hero:hover {
    background: var(--purple-dark);
    box-shadow: 0 12px 32px rgba(56, 31, 71, 0.16), 0 6px 16px rgba(56, 31, 71, 0.1);
  }
  .pg-rootcanal .t-hero-imgframe { border-radius: 16px; overflow: hidden; box-shadow: 0 10px 40px -10px rgb(56 31 71 / 0.2); }
  .pg-rootcanal .t-hero-imgframe img { width: 100%; height: auto; aspect-ratio: 3 / 4; object-fit: cover; display: block; transition: transform 0.5s; }
  .pg-rootcanal .t-hero-imgframe:hover img { transform: scale(1.02); }

  /* ── Quick Answer ── */
  .pg-rootcanal .qa-section { padding: 32px 0; background: #fff; }
  .pg-rootcanal .quick-answer-box {
    background: linear-gradient(to right, rgb(56 31 71 / 0.1), rgb(179 149 95 / 0.1));
    border: 1px solid rgb(179 149 95 / 0.3);
    border-radius: 12px;
    padding: 24px;
    margin-bottom: 32px;
    display: flex; align-items: flex-start; gap: 16px;
  }
  .pg-rootcanal .qa-icon { flex-shrink: 0; color: var(--gold); margin-top: 4px; }
  .pg-rootcanal .quick-answer-box h2 { font-family: inherit; font-size: 1.125rem; font-weight: 600; color: var(--purple); margin-bottom: 8px; }
  .pg-rootcanal .quick-answer-box p { color: var(--gray); line-height: 1.625; }

  /* ── Pain Points ── */
  .pg-rootcanal .pp-section {
    padding: 64px 0;
    background: linear-gradient(to bottom right, hsl(51 22% 76% / 0.2), #fff, hsl(51 22% 76% / 0.1));
  }
  .pg-rootcanal .pp-title {
    font-family: 'Playfair Display', Georgia, serif;
    font-weight: 700; font-size: 1.5rem; line-height: 1.25;
    color: var(--purple); margin-bottom: 40px; text-align: center;
  }
  @media (min-width: 768px) { .pg-rootcanal .pp-title { font-size: 1.875rem; } }
  .pg-rootcanal .pp-list { display: flex; flex-direction: column; gap: 20px; }
  .pg-rootcanal .pp-item {
    display: flex; align-items: flex-start; gap: 16px;
    background: rgb(255 255 255 / 0.8);
    -webkit-backdrop-filter: blur(4px); backdrop-filter: blur(4px);
    padding: 24px; border-radius: 12px;
    border-left: 4px solid var(--purple);
    box-shadow: 0 4px 20px hsl(0 0% 0% / 0.08);
    transition: box-shadow 0.3s;
  }
  .pg-rootcanal .pp-item:hover { box-shadow: 0 10px 40px -10px rgb(56 31 71 / 0.2); }
  .pg-rootcanal .pp-item.b-gold { border-left-color: var(--gold); }
  .pg-rootcanal .pp-item.b-purple-soft { border-left-color: var(--purple-soft); }
  .pg-rootcanal .pp-icon {
    flex-shrink: 0; width: 48px; height: 48px; border-radius: 999px;
    background: rgb(56 31 71 / 0.1);
    display: flex; align-items: center; justify-content: center;
    transition: background-color 0.2s;
  }
  .pg-rootcanal .pp-item:hover .pp-icon { background: rgb(56 31 71 / 0.2); }
  .pg-rootcanal .c-purple { color: var(--purple); }
  .pg-rootcanal .c-gold { color: var(--gold); }
  .pg-rootcanal .c-purple-soft { color: var(--purple-soft); }
  .pg-rootcanal .pp-item strong { display: block; color: var(--purple); font-weight: 600; margin-bottom: 4px; }
  .pg-rootcanal .pp-item span:not(.pp-icon) { color: var(--gray); line-height: 1.625; }
  .pg-rootcanal .pp-conclusion { text-align: center; color: var(--gray); margin-top: 40px; font-size: 1.125rem; line-height: 1.625; }

  /* ── Divider ── */
  .pg-rootcanal .divider { display: flex; align-items: center; justify-content: center; gap: 16px; margin: 32px 0; }
  @media (min-width: 768px) { .pg-rootcanal .divider { margin: 48px 0; } }
  .pg-rootcanal .d-line { flex: 1; height: 1px; background: rgb(179 149 95 / 0.3); max-width: 128px; }
  .pg-rootcanal .d-circle {
    width: 48px; height: 48px; border-radius: 999px;
    background: rgb(179 149 95 / 0.1); color: var(--gold);
    display: flex; align-items: center; justify-content: center;
    transition: transform 0.2s;
  }
  .pg-rootcanal .d-circle:hover { transform: scale(1.1); }

  /* ── Warning Signs ── */
  .pg-rootcanal .ws-section { padding: 64px 0; background: #fff; }
  .pg-rootcanal .ws-grid { display: grid; gap: 32px; }
  @media (min-width: 768px) { .pg-rootcanal .ws-grid { grid-template-columns: repeat(3, 1fr); } }
  .pg-rootcanal .ws-card { text-align: center; }
  .pg-rootcanal .ws-icon {
    width: 64px; height: 64px; border-radius: 999px;
    display: flex; align-items: center; justify-content: center;
    margin: 0 auto 16px;
  }
  .pg-rootcanal .ws-icon.bg-purple { background: rgb(56 31 71 / 0.1); }
  .pg-rootcanal .ws-icon.bg-gold { background: rgb(179 149 95 / 0.1); }
  .pg-rootcanal .ws-card h3 { font-family: inherit; font-size: 1.25rem; font-weight: 600; color: var(--purple); margin-bottom: 12px; }
  .pg-rootcanal .ws-card p { color: var(--gray); }

  /* ── Urgent CTA ── */
  .pg-rootcanal .urgent-cta { padding: 32px 0; background: hsl(51 22% 76% / 0.3); text-align: center; }
  .pg-rootcanal .urgent-cta p { font-size: 1.25rem; color: var(--gray); margin-bottom: 24px; }
  .pg-rootcanal .btn-urgent {
    display: inline-flex; align-items: center; justify-content: center; gap: 8px;
    padding: 16px 32px;
    background: #dc2626; color: #fff;
    font-weight: 600; border-radius: 8px; text-decoration: none;
    box-shadow: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
    transition: all 0.2s;
  }
  .pg-rootcanal .btn-urgent:hover {
    background: #b91c1c;
    box-shadow: 0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1);
    transform: scale(1.05);
  }

  /* ── Doctor Bio ── */
  .pg-rootcanal .bio-section { padding: 64px 0; background: linear-gradient(135deg, hsl(250 100% 98%) 0%, hsl(30 100% 97%) 100%); }
  .pg-rootcanal .bio-title {
    font-family: 'Playfair Display', Georgia, serif; font-weight: 600;
    font-size: 1.875rem; color: var(--purple); text-align: center; margin-bottom: 48px;
  }
  @media (min-width: 768px) { .pg-rootcanal .bio-title { font-size: 2.25rem; } }
  .pg-rootcanal .bio-grid { display: grid; gap: 32px; align-items: start; }
  @media (min-width: 768px) { .pg-rootcanal .bio-grid { grid-template-columns: 300px 1fr; } }
  .pg-rootcanal .bio-photo { margin: 0 auto; }
  @media (min-width: 768px) { .pg-rootcanal .bio-photo { margin: 0; } }
  .pg-rootcanal .bio-photo-frame { position: relative; }
  .pg-rootcanal .bio-photo-frame img { width: 100%; height: auto; border-radius: 16px; box-shadow: 0 10px 40px -10px rgb(56 31 71 / 0.2); }
  .pg-rootcanal .bio-badge {
    position: absolute; bottom: -16px; left: 50%; transform: translateX(-50%);
    background: var(--gold); color: #fff;
    padding: 8px 24px; border-radius: 999px;
    box-shadow: 0 4px 12px rgb(179 149 95 / 0.3);
    font-weight: 600; font-size: 0.875rem; white-space: nowrap;
  }
  .pg-rootcanal .bio-card { background: #fff; padding: 32px; border-radius: 16px; box-shadow: 0 4px 20px hsl(0 0% 0% / 0.08); }
  .pg-rootcanal .bio-card-head { margin-bottom: 24px; }
  .pg-rootcanal .bio-card-head h3 {
    font-family: 'Playfair Display', Georgia, serif; font-weight: 600;
    font-size: 1.875rem; color: var(--purple); margin-bottom: 8px;
  }
  .pg-rootcanal .bio-card-head p { color: var(--gold-dark); font-weight: 500; font-size: 1.125rem; }
  .pg-rootcanal .bio-paragraphs { display: flex; flex-direction: column; gap: 16px; color: var(--gray); line-height: 1.625; }
  .pg-rootcanal .bio-creds { margin-top: 24px; padding-top: 24px; border-top: 1px solid rgb(82 82 82 / 0.2); display: grid; gap: 16px; }
  @media (min-width: 768px) { .pg-rootcanal .bio-creds { grid-template-columns: repeat(2, 1fr); } }
  .pg-rootcanal .bio-cred { display: flex; align-items: flex-start; gap: 12px; }
  .pg-rootcanal .bio-cred-icon { color: var(--gold); margin-top: 4px; flex-shrink: 0; }
  .pg-rootcanal .bio-cred-title { font-weight: 600; color: var(--purple); }
  .pg-rootcanal .bio-cred-desc { font-size: 0.875rem; color: var(--gray); }

  /* ── Timeline ── */
  .pg-rootcanal .tl-section { padding: 64px 0; background: #fff; }
  .pg-rootcanal .tl-title { margin-bottom: 16px; }
  .pg-rootcanal .timeline { position: relative; padding: 32px 0; }
  .pg-rootcanal .tl-vline { position: absolute; left: 24px; top: 0; width: 2px; height: 100%; background: rgb(179 149 95 / 0.3); }
  @media (min-width: 768px) { .pg-rootcanal .tl-vline { left: 32px; } }
  .pg-rootcanal .tl-steps { display: flex; flex-direction: column; gap: 48px; }
  @media (min-width: 768px) { .pg-rootcanal .tl-steps { gap: 64px; } }
  .pg-rootcanal .tl-step { position: relative; display: flex; gap: 16px; }
  @media (min-width: 768px) { .pg-rootcanal .tl-step { gap: 32px; } }
  .pg-rootcanal .tl-num-col { position: relative; flex-shrink: 0; }
  .pg-rootcanal .tl-num {
    font-family: 'Playfair Display', Georgia, serif; font-weight: 700;
    font-size: 1.875rem; line-height: 1; color: rgb(179 149 95 / 0.3);
  }
  @media (min-width: 768px) { .pg-rootcanal .tl-num { font-size: 2.25rem; } }
  @media (min-width: 1024px) { .pg-rootcanal .tl-num { font-size: 3rem; } }
  .pg-rootcanal .tl-dot {
    position: absolute; top: 16px; left: 22px;
    width: 16px; height: 16px; border-radius: 999px;
    background: var(--gold); border: 4px solid #fff;
    box-shadow: 0 1px 2px 0 rgb(0 0 0 / 0.05);
    transform: translateX(-50%);
  }
  @media (min-width: 768px) { .pg-rootcanal .tl-dot { top: 24px; left: 30px; } }
  .pg-rootcanal .tl-content { flex: 1; padding-top: 4px; }
  @media (min-width: 768px) { .pg-rootcanal .tl-content { padding-top: 8px; } }
  .pg-rootcanal .tl-icon { display: inline-flex; margin-bottom: 12px; color: var(--gold); }
  .pg-rootcanal .tl-content h3 { font-family: inherit; font-size: 1.25rem; font-weight: 600; color: var(--purple); margin-bottom: 8px; line-height: 1.25; }
  @media (min-width: 768px) { .pg-rootcanal .tl-content h3 { font-size: 1.5rem; } }
  .pg-rootcanal .tl-content p { color: var(--gray); line-height: 1.625; margin-bottom: 8px; }
  .pg-rootcanal .tl-duration { font-size: 0.875rem; color: var(--gold-dark); font-style: italic; margin-top: 8px; }

  /* ── Informational Section ── */
  .pg-rootcanal .info-section { padding: 64px 0; background: #fff; }
  .pg-rootcanal .info-title { margin-bottom: 32px; }
  .pg-rootcanal .info-body { display: flex; flex-direction: column; gap: 24px; color: var(--gray); line-height: 1.625; }

  /* ── FAQ ── */
  .pg-rootcanal .faq-section { padding: 64px 0; background: hsl(51 22% 76% / 0.2); }
  .pg-rootcanal .faq-title {
    font-family: 'Playfair Display', Georgia, serif; font-weight: 600;
    font-size: 1.875rem; color: var(--purple); text-align: center; margin-bottom: 32px;
  }
  @media (min-width: 768px) { .pg-rootcanal .faq-title { font-size: 2.25rem; } }
  .pg-rootcanal .faq-list { display: flex; flex-direction: column; gap: 16px; }
  .pg-rootcanal .faq-item { background: #fff; border: 1px solid rgb(56 31 71 / 0.2); border-radius: 8px; padding: 0 24px; }
  .pg-rootcanal .faq-item summary {
    list-style: none; cursor: pointer;
    display: flex; align-items: center; justify-content: space-between; gap: 16px;
    text-align: left; font-size: 1rem; font-weight: 600; color: var(--purple);
    padding: 24px 0; transition: color 0.2s;
  }
  .pg-rootcanal .faq-item summary::-webkit-details-marker { display: none; }
  .pg-rootcanal .faq-item summary:hover { color: var(--gold); }
  .pg-rootcanal .chev { display: inline-flex; transition: transform 0.2s; color: currentColor; flex-shrink: 0; }
  .pg-rootcanal details[open] > summary .chev { transform: rotate(180deg); }
  .pg-rootcanal .faq-a { color: var(--gray); line-height: 1.625; padding-bottom: 24px; }

  /* ── Final CTA ── */
  .pg-rootcanal .final-cta {
    padding: 80px 0;
    background: linear-gradient(135deg, #381f47 0%, #b3955f 100%);
    color: #fff; text-align: center;
  }
  .pg-rootcanal .final-cta-head { display: flex; align-items: center; justify-content: center; gap: 12px; margin-bottom: 16px; }
  .pg-rootcanal .final-cta-icon { width: 32px; height: 32px; color: var(--gold); display: inline-flex; align-items: center; justify-content: center; }
  .pg-rootcanal .final-cta h2 {
    font-family: 'Playfair Display', Georgia, serif; font-weight: 600;
    font-size: 1.875rem; color: #fff;
  }
  @media (min-width: 768px) { .pg-rootcanal .final-cta h2 { font-size: 2.25rem; } }
  .pg-rootcanal .final-cta-desc { font-size: 1.25rem; margin-bottom: 32px; opacity: 0.9; max-width: 672px; margin-left: auto; margin-right: auto; }
  .pg-rootcanal .final-cta-btn-wrap { display: flex; flex-direction: column; gap: 16px; justify-content: center; align-items: center; }
  @media (min-width: 640px) { .pg-rootcanal .final-cta-btn-wrap { flex-direction: row; } }
  .pg-rootcanal .btn-final {
    display: inline-flex; align-items: center; gap: 8px;
    background: var(--gold); color: var(--purple);
    padding: 16px 32px; border-radius: 8px;
    font-weight: 600; font-size: 1.125rem; text-decoration: none;
    transition: all 0.2s;
  }
  .pg-rootcanal .btn-final:hover { opacity: 0.9; }
  .pg-rootcanal .final-cta-footnote { margin-top: 24px; font-size: 0.875rem; opacity: 0.75; }

  /* ── Related Treatments ── */
  .pg-rootcanal .rel-section { padding: 48px 0; background: hsl(51 22% 76% / 0.2); }
  .pg-rootcanal .rel-section h2 {
    font-family: 'Playfair Display', Georgia, serif; font-weight: 600;
    font-size: 1.25rem; color: var(--purple); margin-bottom: 24px;
  }
  .pg-rootcanal .rel-grid { display: grid; gap: 16px; }
  @media (min-width: 640px) { .pg-rootcanal .rel-grid { grid-template-columns: repeat(3, 1fr); } }
  .pg-rootcanal .rel-card {
    display: flex; flex-direction: column; gap: 8px;
    background: #fff; padding: 20px; border-radius: 12px;
    border: 1px solid #e5e1d4;
    text-decoration: none;
    transition: all 0.2s;
  }
  .pg-rootcanal .rel-card:hover {
    border-color: rgb(56 31 71 / 0.3);
    box-shadow: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1);
  }
  .pg-rootcanal .rel-title { font-weight: 600; color: var(--purple); font-size: 0.875rem; line-height: 1.375; transition: color 0.2s; }
  .pg-rootcanal .rel-card:hover .rel-title { color: #4a2759; }
  .pg-rootcanal .rel-desc { color: var(--gray); font-size: 0.75rem; line-height: 1.625; flex: 1; }
  .pg-rootcanal .rel-more { display: flex; align-items: center; gap: 4px; color: var(--gold); font-size: 0.75rem; font-weight: 500; margin-top: 4px; }

  /* ── Stats Bar ── */
  .pg-rootcanal .stats-bar {
    background: rgb(56 31 71 / 0.05);
    border-top: 1px solid rgb(56 31 71 / 0.1);
    border-bottom: 1px solid rgb(56 31 71 / 0.1);
  }
  .pg-rootcanal .stats-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 24px; padding: 24px 0; }
  @media (min-width: 768px) { .pg-rootcanal .stats-grid { grid-template-columns: repeat(4, 1fr); gap: 32px; } }
  .pg-rootcanal .stat { display: flex; align-items: center; gap: 12px; justify-content: center; }
  .pg-rootcanal .stat-icon {
    flex-shrink: 0; width: 40px; height: 40px; border-radius: 999px;
    background: rgb(179 149 95 / 0.1); color: var(--gold);
    display: flex; align-items: center; justify-content: center;
  }
  .pg-rootcanal .stat-value { font-size: 1.125rem; font-weight: 600; color: var(--purple); line-height: 1.25; }
  .pg-rootcanal .stat-label { font-size: 0.75rem; color: var(--gray); line-height: 1.25; }
</style>
