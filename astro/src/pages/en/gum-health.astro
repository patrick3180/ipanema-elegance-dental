---
/**
 * /en/gum-health — porte fiel de src/pages/en/EnGumHealthPage.tsx.
 * Copy VERBATIM (tratamento periodontal é feito com periodontista parceiro —
 * a copy do React reflete isso e foi mantida). Layout: SiteLayout lang="en".
 * CTAs WhatsApp = âncoras .js-wa-cta (handler global do Base.astro).
 */
import SiteLayout from '../../layouts/SiteLayout.astro';

// ── Lucide icon paths (inline SVG) ──
const P: Record<string, string> = {
  shield: '<path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>',
  alertCircle: '<circle cx="12" cy="12" r="10"/><line x1="12" x2="12" y1="8" y2="12"/><line x1="12" x2="12.01" y1="16" y2="16"/>',
  activity: '<polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/>',
  arrowRight: '<path d="M5 12h14"/><path d="m12 5 7 7-7 7"/>',
  heart: '<path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/>',
  search: '<circle cx="11" cy="11" r="8"/><path d="m21 21-4.3-4.3"/>',
  helpCircle: '<circle cx="12" cy="12" r="10"/><path d="M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3"/><path d="M12 17h.01"/>',
  checkCircle: '<path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/>',
  checkCircle2: '<circle cx="12" cy="12" r="10"/><path d="m9 12 2 2 4-4"/>',
  calendar: '<path d="M8 2v4"/><path d="M16 2v4"/><rect width="18" height="18" x="3" y="4" rx="2"/><path d="M3 10h18"/>',
  users: '<path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M22 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/>',
  award: '<circle cx="12" cy="8" r="6"/><path d="M15.477 12.89 17 22l-5-3-5 3 1.523-9.11"/>',
  star: '<polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/>',
  chevronDown: '<path d="m6 9 6 6 6-6"/>',
  chevronRight: '<path d="m9 18 6-6-6-6"/>',
};
const ic = (name: string, cls = '', size = 24) =>
  `<svg xmlns="http://www.w3.org/2000/svg" width="${size}" height="${size}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="${cls}" aria-hidden="true">${P[name]}</svg>`;

// ── FAQs (6 — verbatim do React) ──
const faqs = [
  { question: 'Is it normal for gums to bleed when brushing?', answer: "No. Gum bleeding is a sign of inflammation, even if there's no pain. It may be gingivitis (reversible) or periodontitis (more advanced). It's worth seeking an evaluation to identify the cause and treat it before it progresses." },
  { question: 'Can periodontitis be cured?', answer: "Periodontitis is manageable, but it's not 'curable' in the sense that it disappears on its own. With proper treatment and periodic maintenance, it's possible to stabilize the disease and prevent progression. Discipline in maintenance appointments is essential." },
  { question: 'Can I get veneers or implants if I have gum disease?', answer: 'First, the gums need to be treated. Veneers, lenses, and implants require a healthy gum foundation to function well and last. Dr. Carla integrates periodontal treatment into the case planning — one step at a time.' },
  { question: 'Can gum recession be treated?', answer: 'It depends on the cause and extent. In some cases, gum graft procedures can cover the exposed root. In others, the goal is to stabilize the situation and prevent further progression. Clinical evaluation determines the best approach.' },
  { question: 'How often should I get a dental cleaning?', answer: 'For most people, every 6 months. Patients with a history of periodontal disease may need shorter intervals — every 3 or 4 months. The ideal frequency is determined individually.' },
  { question: 'Can bad breath be a gum problem?', answer: "Yes. Persistent bad breath (halitosis) is frequently associated with periodontal disease — bacteria accumulated in gum pockets produce foul-smelling compounds. If bad breath doesn't improve with careful oral hygiene, it's worth investigating." },
];

// ── Schemas (verbatim do SEOHead React) ──
const schemas = [
  JSON.stringify({
    "@context": "https://schema.org", "@type": "MedicalProcedure",
    "name": "Periodontal Treatment (Gum Health)",
    "description": "Diagnosis and treatment of gum diseases with follow-up integrated into the comprehensive treatment plan",
    "procedureType": "Dental", "inLanguage": "en",
    "provider": { "@type": "Dentist", "name": "Dr. Carla Christoph", "telephone": "+5521993304045",
      "address": { "@type": "PostalAddress", "streetAddress": "Rua Visconde de Pirajá, 550 - Suite 1107", "addressLocality": "Ipanema", "addressRegion": "RJ", "postalCode": "22410-901", "addressCountry": "BR" }
    }
  }),
  JSON.stringify({
    "@context": "https://schema.org", "@type": "FAQPage", "inLanguage": "en",
    "mainEntity": faqs.map((faq) => ({ "@type": "Question", "name": faq.question, "acceptedAnswer": { "@type": "Answer", "text": faq.answer } }))
  }),
  JSON.stringify({
    "@context": "https://schema.org", "@type": "WebPage",
    "name": "Gum Health & Periodontics in Ipanema",
    "url": "https://dracarlachristoph.com/en/gum-health",
    "speakable": { "@type": "SpeakableSpecification", "cssSelector": [".treatment-hero-title", ".treatment-hero-description", ".quick-answer-box"] },
    "inLanguage": "en"
  }),
  // BreadcrumbList — gerado pelo TreatmentHero no React
  JSON.stringify({
    "@context": "https://schema.org", "@type": "BreadcrumbList",
    "itemListElement": [
      { "@type": "ListItem", "position": 1, "name": "Home", "item": "https://dracarlachristoph.com/en" },
      { "@type": "ListItem", "position": 2, "name": "Treatments", "item": "https://dracarlachristoph.com/en" },
      { "@type": "ListItem", "position": 3, "name": "Gum Health" }
    ]
  }),
];

// ── WhatsApp CTAs (mensagens verbatim do React) ──
const WA = 'https://wa.me/5521993304045?text=';
const waHero = WA + encodeURIComponent('Hello! I would like to schedule a consultation about Gum Health in Ipanema.');
const waFinal = WA + encodeURIComponent('Hello! I saw the page about gum health and would like to schedule a periodontal evaluation with Dr. Carla Christoph.');

// ── Timeline (verbatim) ──
const timelineSteps = [
  { number: '1', icon: 'search', title: 'Evaluation with Dr. Carla', description: 'Detailed clinical gum exam, periodontal probing, and X-rays when needed. Identification of the problem stage.', duration: '1st Appointment' },
  { number: '2', icon: 'shield', title: 'Periodontal Treatment', description: 'Deep cleaning (scaling), treatment with a specialized periodontist when indicated. Control of infection and inflammation.', duration: '1-4 sessions' },
  { number: '3', icon: 'checkCircle', title: 'Re-Evaluation', description: 'After treatment, Dr. Carla re-evaluates the gum response and defines next steps — maintenance, complementary treatments, or referral for aesthetic procedures.', duration: '30-45 days' },
  { number: '4', icon: 'heart', title: 'Periodic Maintenance', description: 'Periodontal disease is manageable but requires follow-up. Regular maintenance appointments prevent recurrence and maintain gum health.', duration: 'Every 3-6 months' },
];
---

<SiteLayout
  title="Gum Health & Periodontics in Ipanema | Dr. Carla Christoph, Rio de Janeiro"
  description="Periodontal treatment and gum health in Ipanema. Prevention and treatment of gingivitis and periodontitis. Dr. Carla Christoph, CRO-RJ 27.509."
  canonical="https://dracarlachristoph.com/en/gum-health"
  ogImage="https://dracarlachristoph.com/lovable-uploads/dra-carla-jaleco-bracos-cruzados.webp"
  preloadImage="/lovable-uploads/dra-carla-jaleco-bracos-cruzados.webp"
  schemas={schemas}
  lang="en"
>
  <Fragment slot="head">
    <link rel="alternate" hreflang="pt-BR" href="https://dracarlachristoph.com/saude-da-gengiva" />
    <link rel="alternate" hreflang="en" href="https://dracarlachristoph.com/en/gum-health" />
    <link rel="alternate" hreflang="x-default" href="https://dracarlachristoph.com/saude-da-gengiva" />
  </Fragment>

  <main id="main-content">
    <!-- ═══ Hero ═══ -->
    <section class="t-hero" aria-label="Treatment hero">
      <div class="content-c">
        <div class="hero-grid">
          <div class="hero-content">
            <nav class="crumbs" aria-label="Breadcrumb">
              <a href="/en">Home</a>
              <Fragment set:html={ic('chevronRight', 'crumb-sep', 14)} />
              <a href="/en">Treatments</a>
              <Fragment set:html={ic('chevronRight', 'crumb-sep', 14)} />
              <span class="crumb-cur">Gum Health</span>
            </nav>
            <h1 class="hero-title treatment-hero-title">Gum Health in Ipanema</h1>
            <p class="hero-sub">Periodontics with Follow-Up by Dr. Carla Christoph</p>
            <p class="hero-desc treatment-hero-description">Healthy gums are the foundation of any dental treatment. Bleeding, recession, and persistent bad breath deserve attention before they become bigger problems.</p>
            <div class="hero-badges">
              <span class="badge">Periodontal Health</span>
              <span class="badge">Integrated Care</span>
              <span class="badge">CRO-RJ 27.509</span>
            </div>
            <div class="hero-cta-wrap">
              <a class="btn-hero js-wa-cta" data-label="en-gum-health_hero_whatsapp" href={waHero} target="_blank" rel="noopener" aria-label="Schedule consultation via WhatsApp">
                <Fragment set:html={ic('calendar', 'mr8', 20)} />
                Book Your Consultation
              </a>
            </div>
          </div>
          <div class="hero-img">
            <div class="hero-img-frame">
              <img src="/lovable-uploads/dra-carla-jaleco-bracos-cruzados.webp" alt="Dr. Carla Christoph - Gum Health in Ipanema Specialist" width="800" height="1200" loading="eager" fetchpriority="high" />
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- ═══ Quick Answer ═══ -->
    <section class="qa-section">
      <div class="container-c">
        <div class="qa-box quick-answer-box">
          <div class="qa-ic"><Fragment set:html={ic('checkCircle2', 'gold', 24)} /></div>
          <div>
            <h2 class="qa-title">Quick Answer</h2>
            <p class="qa-text">Gum health (periodontics) treats gum diseases such as gingivitis and periodontitis, which cause bleeding, recession, and bad breath. At Dr. Carla Christoph's office in Ipanema (CRO-RJ 27.509), we perform periodontal evaluation, professional cleaning, and treatment of gum pockets. Gingivitis is reversible; periodontitis is manageable with treatment and periodic maintenance. For low-risk patients, cleaning every 6 months is recommended; for periodontal disease cases, every 3-4 months to maintain stability.</p>
          </div>
        </div>
      </div>
    </section>

    <div class="divider"><span class="d-line"></span><span class="d-circle"><Fragment set:html={ic('alertCircle', '', 20)} /></span><span class="d-line"></span></div>

    <!-- ═══ Empathetic Pain Points (com callout) ═══ -->
    <section class="pain-section">
      <div class="narrow-c">
        <h2 class="pain-title">Do you relate to any of these situations?</h2>
        <div class="pain-list">
          <div class="pain-card b-purple">
            <div class="pain-ic"><Fragment set:html={ic('activity', 'purple', 24)} /></div>
            <div>
              <strong>Gums that bleed when brushing or flossing</strong>
              <span>and you've been living with it for years thinking it's "just normal"?</span>
            </div>
          </div>
          <div class="pain-card b-gold">
            <div class="pain-ic"><Fragment set:html={ic('alertCircle', 'gold', 24)} /></div>
            <div>
              <strong>Persistent bad breath despite good brushing</strong>
              <span>that causes discomfort in social and professional interactions?</span>
            </div>
          </div>
          <div class="pain-card b-purple-soft">
            <div class="pain-ic"><Fragment set:html={ic('heart', 'purple-soft', 24)} /></div>
            <div>
              <strong>Receding gums or teeth that seem "longer"</strong>
              <span>with sensitivity to cold that keeps getting worse over time?</span>
            </div>
          </div>
        </div>
        <p class="pain-conclusion">Periodontal disease is silent — it usually doesn't hurt in the early stages.<br class="md-br" /> When detected early, it's treatable and manageable.</p>
        <div class="callout-wrap">
          <div class="callout">
            <div class="callout-ic"><Fragment set:html={ic('alertCircle', 'gold', 20)} /></div>
            <div>
              <p class="callout-t">Important</p>
              <p class="callout-d">Periodontitis takes 2 to 5 years to evolve from gingivitis, but once established, bone loss is irreversible. Treatment stops progression — that's why early detection is critical.</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <div class="divider"><span class="d-line"></span><span class="d-circle"><Fragment set:html={ic('shield', '', 20)} /></span><span class="d-line"></span></div>

    <!-- ═══ Common Gum Problems — 3 Cards ═══ -->
    <section class="prob-section">
      <div class="container-c">
        <h2 class="center-title">Most Common Gum Problems</h2>
        <p class="center-sub">Understand what each sign may mean</p>
        <div class="prob-grid">
          <div class="prob-card">
            <div class="prob-ic bg-purple10"><Fragment set:html={ic('activity', 'purple', 32)} /></div>
            <h3>Gingivitis</h3>
            <p>Initial gum inflammation. Bleeding when brushing, redness, and swelling. Reversible with proper treatment and good hygiene. Does not cause bone loss.</p>
          </div>
          <div class="prob-card">
            <div class="prob-ic bg-gold10"><Fragment set:html={ic('alertCircle', 'gold', 32)} /></div>
            <h3>Periodontitis</h3>
            <p>Progression of untreated gingivitis. Affects the supporting bone around teeth. Can cause tooth mobility and, in advanced cases, tooth loss. Manageable but requires follow-up.</p>
          </div>
          <div class="prob-card">
            <div class="prob-ic bg-purple10"><Fragment set:html={ic('arrowRight', 'purple', 32)} /></div>
            <h3>Gum Recession</h3>
            <p>The gum recedes, exposing part of the tooth root. Causes sensitivity and changes smile aesthetics. May be associated with aggressive brushing, bruxism, or periodontal disease.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- ═══ Doctor Bio ═══ -->
    <section class="bio-section">
      <div class="container-c">
        <h2 class="bio-sec-title">Healthy Gums Are a Prerequisite</h2>
        <div class="bio-wrap">
          <div class="bio-photo-col">
            <div class="bio-photo">
              <img src="/lovable-uploads/dra-carla-jaleco-bracos-cruzados.webp" alt="Dr. Carla Christoph - Dental Specialist" width="800" height="1200" loading="lazy" decoding="async" />
              <div class="bio-badge">Integrated Care</div>
            </div>
          </div>
          <div class="bio-card">
            <div class="bio-name-wrap">
              <h3>Dr. Carla Christoph</h3>
              <p class="bio-sub">CRO-RJ 27.509 | Specialist in Prosthodontics and Implant Dentistry</p>
            </div>
            <div class="bio-paras">
              <p>With over two decades in Ipanema, Dr. Carla has built her reputation by treating each patient individually, with time and attention. Her background includes 8 years as a military dentist at the Brazilian Navy Dental Center, an experience that brought discipline and precision to her clinical practice.</p>
              <p>Dr. Carla treats periodontal health as the foundation for any procedure. It doesn't make sense to invest in <a href="/en/veneers-and-lenses" class="bio-link">veneers</a>, <a href="/en/dental-implants" class="bio-link">implants</a>, or <a href="/en/dental-prosthetics" class="bio-link">prosthetics</a> on compromised gums. Periodontal treatment is done in partnership with a specialized periodontist, and Dr. Carla oversees all progress to integrate gum care into the patient's complete treatment plan.</p>
            </div>
            <div class="bio-creds">
              <div class="cred"><Fragment set:html={ic('checkCircle', 'gold cred-ic', 20)} /><div><p class="cred-t">Integrated vision</p><p class="cred-d">Gum health as the foundation for any aesthetic treatment</p></div></div>
              <div class="cred"><Fragment set:html={ic('checkCircle', 'gold cred-ic', 20)} /><div><p class="cred-t">Specialized partnership</p><p class="cred-d">Treatment with periodontist and Dr. Carla's oversight</p></div></div>
              <div class="cred"><Fragment set:html={ic('checkCircle', 'gold cred-ic', 20)} /><div><p class="cred-t">Experience</p><p class="cred-d">20+ years, including 8 in the Navy</p></div></div>
              <div class="cred"><Fragment set:html={ic('checkCircle', 'gold cred-ic', 20)} /><div><p class="cred-t">Approach</p><p class="cred-d">Individualized appointments, minimum 1 hour</p></div></div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <div class="divider"><span class="d-line"></span><span class="d-circle"><Fragment set:html={ic('search', '', 20)} /></span><span class="d-line"></span></div>

    <!-- ═══ Process Timeline ═══ -->
    <section class="sec-white">
      <div class="container-c">
        <h2 class="center-title">How We Care for Your Gum Health</h2>
        <div class="med-c">
          <div class="timeline">
            <div class="tl-line" aria-hidden="true"></div>
            <div class="tl-steps" role="list">
              {timelineSteps.map((step) => (
                <div class="tl-step" role="listitem">
                  <div class="tl-num-col">
                    <div class="tl-num">{step.number}</div>
                    <div class="tl-dot" aria-hidden="true"></div>
                  </div>
                  <div class="tl-content">
                    <div class="tl-ic"><Fragment set:html={ic(step.icon, '', 24)} /></div>
                    <h3>{step.title}</h3>
                    <p>{step.description}</p>
                    <p class="tl-duration">Duração: {step.duration}</p>
                  </div>
                </div>
              ))}
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- ═══ Gum & General Health Connection ═══ -->
    <section class="sec-white">
      <div class="container-c">
        <h2 class="center-title conn-title">The Connection Between Gums and Overall Health</h2>
        <div class="conn-body">
          <p>Gum health is connected to the health of the body as a whole. Studies show an association between periodontal disease and conditions such as diabetes, cardiovascular disease, and pregnancy complications. Treating gums isn't just an aesthetic concern — it's caring for your systemic health.</p>
          <p>In practice, what we see most at the office are patients who had been living with gum bleeding for years without realizing its severity. After treatment, they report not only improvement in their mouth but an overall feeling of health care they had been postponing.</p>
        </div>
      </div>
    </section>

    <div class="divider"><span class="d-line"></span><span class="d-circle"><Fragment set:html={ic('helpCircle', '', 20)} /></span><span class="d-line"></span></div>

    <!-- ═══ FAQ ═══ -->
    <section class="faq-section">
      <div class="container-c">
        <h2 class="faq-title">Questions About Gum Health</h2>
        <div class="faq-list">
          {faqs.map((faq) => (
            <details class="faq-item">
              <summary>{faq.question}<Fragment set:html={ic('chevronDown', 'acc-chev', 16)} /></summary>
              <div class="faq-answer"><p>{faq.answer}</p></div>
            </details>
          ))}
        </div>
      </div>
    </section>

    <!-- ═══ Final CTA ═══ -->
    <section class="final-cta">
      <div class="container-c center">
        <div class="fc-head">
          <span class="fc-ic"><Fragment set:html={ic('heart', '', 32)} /></span>
          <h2>Take Care of Your Smile's Foundation</h2>
        </div>
        <p class="fc-desc">Schedule your periodontal evaluation. Early treatment makes all the difference.</p>
        <div class="fc-btn-wrap">
          <a class="fc-btn fc-gold js-wa-cta" data-label="en_gum_health_service_page_cta" href={waFinal} target="_blank" rel="noopener">
            <Fragment set:html={ic('arrowRight', 'mr8', 20)} />
            Schedule Evaluation
          </a>
        </div>
        <p class="fc-footnote">WhatsApp: (21) 99330-4045 | 24h Service</p>
      </div>
    </section>

    <!-- ═══ Related Treatments (InternalLinkingOptimizer) ═══ -->
    <section class="rel-section">
      <div class="med-c">
        <h2 class="rel-title">Related Treatments</h2>
        <div class="rel-grid">
          {[
            { title: 'Dental Implants', url: '/en/dental-implants', description: 'Implants require healthy gums' },
            { title: 'General Dentistry', url: '/en/general-dentistry', description: 'Prevention and periodontal maintenance' },
            { title: 'Veneers & Lenses', url: '/en/veneers-and-lenses', description: 'Aesthetics with a healthy foundation' },
          ].map((link) => (
            <a href={link.url} class="rel-card">
              <span class="rel-t">{link.title}</span>
              <span class="rel-d">{link.description}</span>
              <span class="rel-more">Learn more <Fragment set:html={ic('arrowRight', '', 12)} /></span>
            </a>
          ))}
        </div>
      </div>
    </section>

    <!-- ═══ Stats Bar ═══ -->
    <section class="stats-bar" aria-label="Clinic statistics">
      <div class="container-c">
        <div class="stats-grid">
          <div class="stat"><div class="stat-ic"><Fragment set:html={ic('calendar', 'gold', 20)} /></div><div><p class="stat-v">20+</p><p class="stat-l">Years of Experience</p></div></div>
          <div class="stat"><div class="stat-ic"><Fragment set:html={ic('users', 'gold', 20)} /></div><div><p class="stat-v">4.000+</p><p class="stat-l">Patients Served</p></div></div>
          <div class="stat"><div class="stat-ic"><Fragment set:html={ic('award', 'gold', 20)} /></div><div><p class="stat-v">CRO-RJ</p><p class="stat-l">27.509</p></div></div>
          <div class="stat"><div class="stat-ic"><Fragment set:html={ic('star', 'gold star-fill', 20)} /></div><div><p class="stat-v">5.0 ★</p><p class="stat-l">Google Rating</p></div></div>
        </div>
      </div>
    </section>
  </main>
</SiteLayout>

<style>
  /* ══ Tokens/utilitários (porte do tailwind/index.css do SPA) ══ */
  main { --gold-dark: #9d7f4f; --beige-hsl: 51 22% 76%; }
  .container-c { max-width: 1280px; margin: 0 auto; padding-left: 24px; padding-right: 24px; }
  .content-c { max-width: 1280px; margin: 0 auto; padding-left: 24px; padding-right: 24px; }
  .med-c { max-width: 896px; margin: 0 auto; }
  .narrow-c { max-width: 896px; margin: 0 auto; padding-left: 16px; padding-right: 16px; }
  .center { text-align: center; }
  .md-br { display: none; }
  @media (min-width: 768px) { .md-br { display: inline; } }

  :global(.gold) { color: var(--dental-gold); }
  :global(.purple) { color: var(--dental-purple); }
  :global(.purple-soft) { color: var(--dental-purple-soft); }
  :global(.mr8) { margin-right: 8px; flex-shrink: 0; }
  :global(.star-fill) { fill: var(--dental-gold); }

  /* ══ Hero ══ */
  .t-hero {
    background: linear-gradient(135deg, hsl(250 100% 98%) 0%, hsl(30 100% 97%) 100%);
    padding-top: 112px;
    padding-bottom: 64px;
  }
  @media (min-width: 768px) { .t-hero { padding-bottom: 80px; } }
  @media (min-width: 1024px) { .t-hero { padding-bottom: 96px; } }
  .hero-grid { display: grid; gap: 32px; align-items: center; }
  .hero-img { order: -1; }
  @media (min-width: 1024px) {
    .hero-grid { grid-template-columns: repeat(5, minmax(0, 1fr)); gap: 48px; }
    .hero-content { grid-column: span 3; }
    .hero-img { grid-column: span 2; order: 1; }
  }
  .hero-content > * + * { margin-top: 24px; }
  .crumbs { display: flex; align-items: center; flex-wrap: wrap; gap: 6px; font-size: 0.875rem; opacity: 0.7; }
  .crumbs a { color: var(--dental-purple-soft); text-decoration: none; transition: color 0.2s; }
  .crumbs a:hover { color: var(--dental-purple); }
  .crumb-cur { color: var(--dental-purple); font-weight: 500; }
  :global(.crumb-sep) { color: var(--dental-gray); }
  .hero-title { font-family: 'Playfair Display', Georgia, serif; font-weight: 400; font-size: 2.25rem; line-height: 1.15; color: var(--dental-purple); }
  @media (min-width: 768px) { .hero-title { font-size: 3rem; } }
  @media (min-width: 1024px) { .hero-title { font-size: 3.75rem; } }
  .hero-sub { font-size: 1.25rem; color: var(--dental-purple-soft); font-weight: 500; }
  @media (min-width: 768px) { .hero-sub { font-size: 1.5rem; } }
  .hero-desc { font-size: 1.125rem; color: var(--dental-gray); line-height: 1.7; max-width: 42rem; }
  .hero-badges { display: flex; flex-wrap: wrap; gap: 12px; }
  .badge {
    display: inline-flex; align-items: center; border-radius: 999px;
    background: rgb(179 149 95 / 0.1); color: var(--gold-dark);
    border: 1px solid rgb(179 149 95 / 0.2);
    padding: 6px 16px; font-size: 0.875rem; font-weight: 500;
  }
  .hero-cta-wrap { padding-top: 16px; }
  .btn-hero {
    display: inline-flex; align-items: center; justify-content: center;
    background: var(--dental-purple); color: #fff; text-decoration: none;
    padding: 12px 24px; border-radius: 6px; font-size: 1rem; font-weight: 500;
    box-shadow: 0 4px 20px hsl(0 0% 0% / 0.08); transition: all 0.3s;
  }
  .btn-hero:hover { background: #2a1735; box-shadow: 0 12px 32px rgba(56, 31, 71, 0.16), 0 6px 16px rgba(56, 31, 71, 0.1); }
  .hero-img-frame { border-radius: 16px; overflow: hidden; box-shadow: 0 10px 40px -10px rgb(56 31 71 / 0.2); }
  .hero-img-frame img { width: 100%; height: auto; transition: transform 0.5s; }
  .hero-img-frame:hover img { transform: scale(1.02); }

  /* ══ Quick Answer ══ */
  .qa-section { padding: 32px 0; background: #fff; }
  .qa-box {
    display: flex; align-items: flex-start; gap: 16px;
    background: linear-gradient(to right, rgb(56 31 71 / 0.1), rgb(179 149 95 / 0.1));
    border: 1px solid rgb(179 149 95 / 0.3); border-radius: 12px; padding: 24px; margin-bottom: 32px;
  }
  .qa-ic { flex-shrink: 0; margin-top: 4px; }
  .qa-title { font-family: Montserrat, sans-serif; font-size: 1.125rem; font-weight: 600; color: var(--dental-purple); margin-bottom: 8px; }
  .qa-text { color: var(--dental-gray); line-height: 1.7; }

  /* ══ Pain Points ══ */
  .pain-section { padding: 64px 0; background: linear-gradient(to bottom right, hsl(var(--beige-hsl) / 0.2), #fff, hsl(var(--beige-hsl) / 0.1)); }
  .pain-title { font-family: 'Playfair Display', Georgia, serif; font-weight: 600; font-size: 1.5rem; color: var(--dental-purple); margin-bottom: 40px; text-align: center; }
  @media (min-width: 768px) { .pain-title { font-size: 1.875rem; } }
  .pain-list > * + * { margin-top: 20px; }
  .pain-card {
    display: flex; align-items: flex-start; gap: 16px;
    background: rgb(255 255 255 / 0.8); backdrop-filter: blur(4px);
    padding: 24px; border-radius: 12px; border-left: 4px solid var(--dental-purple);
    box-shadow: 0 4px 20px hsl(0 0% 0% / 0.08); transition: box-shadow 0.3s;
  }
  .pain-card:hover { box-shadow: 0 10px 40px -10px rgb(56 31 71 / 0.2); }
  .pain-card.b-gold { border-left-color: var(--dental-gold); }
  .pain-card.b-purple-soft { border-left-color: var(--dental-purple-soft); }
  .pain-ic { flex-shrink: 0; width: 48px; height: 48px; border-radius: 999px; background: rgb(56 31 71 / 0.1); display: flex; align-items: center; justify-content: center; }
  .pain-card strong { display: block; color: var(--dental-purple); font-weight: 600; margin-bottom: 4px; }
  .pain-card span { color: var(--dental-gray); line-height: 1.7; }
  .pain-conclusion { text-align: center; color: var(--dental-gray); margin-top: 40px; font-size: 1.125rem; line-height: 1.7; }
  .callout-wrap { max-width: 672px; margin: 32px auto 0; }
  .callout { display: flex; align-items: flex-start; gap: 16px; background: #fff; padding: 24px; border-radius: 12px; border: 2px solid var(--dental-gold); box-shadow: 0 10px 40px -10px rgb(56 31 71 / 0.2); }
  .callout-ic { flex-shrink: 0; width: 40px; height: 40px; border-radius: 999px; background: rgb(179 149 95 / 0.15); display: flex; align-items: center; justify-content: center; }
  .callout-t { color: var(--dental-purple); font-weight: 600; margin-bottom: 8px; }
  .callout-d { color: var(--dental-gray); line-height: 1.7; font-size: 0.875rem; }

  /* ══ Divider ══ */
  .divider { display: flex; align-items: center; justify-content: center; gap: 16px; margin: 32px 0; }
  @media (min-width: 768px) { .divider { margin: 48px 0; } }
  .d-line { flex: 1; height: 1px; background: rgb(179 149 95 / 0.3); max-width: 128px; }
  .d-circle { width: 48px; height: 48px; border-radius: 999px; background: rgb(179 149 95 / 0.1); display: flex; align-items: center; justify-content: center; color: var(--dental-gold); transition: transform 0.3s; }
  .d-circle:hover { transform: scale(1.1); }

  /* ══ Seções genéricas ══ */
  .sec-white { padding: 64px 0; background: #fff; }
  .center-title { font-family: 'Playfair Display', Georgia, serif; font-weight: 600; font-size: 1.875rem; color: var(--dental-purple); text-align: center; margin-bottom: 16px; }
  @media (min-width: 768px) { .center-title { font-size: 2.25rem; } }
  .center-sub { text-align: center; color: var(--dental-gray); margin: 0 auto 48px; max-width: 42rem; }

  /* ══ Common Gum Problems ══ */
  .prob-section { padding: 64px 0; background: #fff; }
  .prob-grid { display: grid; gap: 32px; max-width: 1024px; margin: 0 auto; }
  @media (min-width: 768px) { .prob-grid { grid-template-columns: repeat(3, minmax(0, 1fr)); } }
  .prob-card { text-align: center; }
  .prob-ic { width: 64px; height: 64px; border-radius: 999px; display: flex; align-items: center; justify-content: center; margin: 0 auto 16px; }
  .bg-purple10 { background: rgb(56 31 71 / 0.1); }
  .bg-gold10 { background: rgb(179 149 95 / 0.1); }
  .prob-card h3 { font-family: Montserrat, sans-serif; font-size: 1.25rem; font-weight: 600; color: var(--dental-purple); margin-bottom: 12px; }
  .prob-card p { color: var(--dental-gray); line-height: 1.7; }

  /* ══ Doctor Bio ══ */
  .bio-section { padding: 64px 0; background: linear-gradient(135deg, hsl(250 100% 98%) 0%, hsl(30 100% 97%) 100%); }
  .bio-sec-title { font-family: 'Playfair Display', Georgia, serif; font-weight: 600; font-size: 1.875rem; color: var(--dental-purple); margin-bottom: 48px; text-align: center; }
  @media (min-width: 768px) { .bio-sec-title { font-size: 2.25rem; } }
  .bio-wrap { max-width: 1024px; margin: 0 auto; display: grid; gap: 32px; align-items: start; }
  @media (min-width: 768px) { .bio-wrap { grid-template-columns: 300px 1fr; } }
  .bio-photo-col { margin: 0 auto; }
  @media (min-width: 768px) { .bio-photo-col { margin: 0; } }
  .bio-photo { position: relative; }
  .bio-photo img { width: 100%; border-radius: 16px; box-shadow: 0 10px 40px -10px rgb(56 31 71 / 0.2); }
  .bio-badge {
    position: absolute; bottom: -16px; left: 50%; transform: translateX(-50%);
    background: var(--dental-gold); color: #fff; padding: 8px 24px; border-radius: 999px;
    box-shadow: 0 4px 12px rgb(179 149 95 / 0.3); font-weight: 600; font-size: 0.875rem; white-space: nowrap;
  }
  .bio-card { background: #fff; padding: 32px; border-radius: 16px; box-shadow: 0 4px 20px hsl(0 0% 0% / 0.08); }
  .bio-name-wrap { margin-bottom: 24px; }
  .bio-name-wrap h3 { font-family: 'Playfair Display', Georgia, serif; font-weight: 600; font-size: 1.875rem; color: var(--dental-purple); margin-bottom: 8px; }
  .bio-sub { color: var(--gold-dark); font-weight: 500; font-size: 1.125rem; }
  .bio-paras { color: var(--dental-gray); line-height: 1.7; }
  .bio-paras > * + * { margin-top: 16px; }
  .bio-link { color: var(--dental-gold); font-weight: 500; text-decoration: none; transition: color 0.2s; }
  .bio-link:hover { color: var(--dental-purple); }
  .bio-creds { margin-top: 24px; padding-top: 24px; border-top: 1px solid rgb(107 107 107 / 0.2); display: grid; gap: 16px; }
  @media (min-width: 768px) { .bio-creds { grid-template-columns: repeat(2, minmax(0, 1fr)); } }
  .cred { display: flex; align-items: flex-start; gap: 12px; }
  :global(.cred-ic) { margin-top: 4px; flex-shrink: 0; }
  .cred-t { font-weight: 600; color: var(--dental-purple); }
  .cred-d { font-size: 0.875rem; color: var(--dental-gray); }

  /* ══ Timeline ══ */
  .timeline { position: relative; padding: 32px 0; }
  .tl-line { position: absolute; left: 24px; top: 0; width: 2px; height: 100%; background: rgb(179 149 95 / 0.3); }
  @media (min-width: 768px) { .tl-line { left: 32px; } }
  .tl-steps > * + * { margin-top: 48px; }
  @media (min-width: 768px) { .tl-steps > * + * { margin-top: 64px; } }
  .tl-step { position: relative; display: flex; gap: 16px; }
  @media (min-width: 768px) { .tl-step { gap: 32px; } }
  .tl-num-col { position: relative; flex-shrink: 0; }
  .tl-num { font-size: 1.875rem; font-family: 'Playfair Display', Georgia, serif; font-weight: 700; color: rgb(179 149 95 / 0.3); line-height: 1; }
  @media (min-width: 768px) { .tl-num { font-size: 2.25rem; } }
  @media (min-width: 1024px) { .tl-num { font-size: 3rem; } }
  .tl-dot { position: absolute; top: 16px; left: 22px; width: 16px; height: 16px; border-radius: 999px; background: var(--dental-gold); border: 4px solid #fff; box-shadow: 0 1px 2px rgb(0 0 0 / 0.05); transform: translateX(-50%); }
  @media (min-width: 768px) { .tl-dot { top: 24px; left: 30px; } }
  .tl-content { flex: 1; padding-top: 4px; }
  @media (min-width: 768px) { .tl-content { padding-top: 8px; } }
  .tl-ic { margin-bottom: 12px; color: var(--dental-gold); }
  .tl-content h3 { font-family: Montserrat, sans-serif; font-size: 1.25rem; font-weight: 600; color: var(--dental-purple); margin-bottom: 8px; line-height: 1.3; }
  @media (min-width: 768px) { .tl-content h3 { font-size: 1.5rem; } }
  .tl-content p { color: var(--dental-gray); line-height: 1.7; margin-bottom: 8px; }
  .tl-duration { font-size: 0.875rem !important; color: var(--gold-dark) !important; font-style: italic; margin-top: 8px; }

  /* ══ Connection section ══ */
  .conn-title { margin-bottom: 32px; }
  .conn-body { max-width: 768px; margin: 0 auto; color: var(--dental-gray); line-height: 1.7; }
  .conn-body > * + * { margin-top: 24px; }

  /* ══ FAQ ══ */
  .faq-section { padding: 64px 0; background: hsl(var(--beige-hsl) / 0.2); }
  .faq-title { font-family: 'Playfair Display', Georgia, serif; font-weight: 600; font-size: 1.875rem; color: var(--dental-purple); margin-bottom: 32px; text-align: center; }
  @media (min-width: 768px) { .faq-title { font-size: 2.25rem; } }
  .faq-list { max-width: 896px; margin: 0 auto; }
  .faq-list > * + * { margin-top: 16px; }
  .faq-item { background: #fff; border-radius: 8px; border: 1px solid rgb(56 31 71 / 0.2); padding: 0 24px; }
  .faq-item summary {
    display: flex; align-items: center; justify-content: space-between; gap: 12px;
    padding: 24px 0; cursor: pointer; list-style: none; text-align: left;
    font-size: 1rem; font-weight: 600; color: var(--dental-purple); transition: color 0.2s;
  }
  .faq-item summary:hover { color: var(--dental-gold); }
  .faq-item summary::-webkit-details-marker { display: none; }
  :global(.acc-chev) { flex-shrink: 0; color: var(--dental-gray); transition: transform 0.2s; }
  .faq-item[open] summary :global(.acc-chev) { transform: rotate(180deg); }
  .faq-answer { padding-bottom: 24px; }
  .faq-answer p { color: var(--dental-gray); line-height: 1.7; }

  /* ══ Final CTA ══ */
  .final-cta { padding: 80px 0; background: linear-gradient(135deg, #381F47 0%, #B3955F 100%); color: #fff; }
  .fc-head { display: flex; align-items: center; justify-content: center; gap: 12px; margin-bottom: 16px; }
  .fc-ic { width: 32px; height: 32px; color: var(--dental-gold); display: inline-flex; align-items: center; justify-content: center; flex-shrink: 0; }
  .fc-head h2 { font-family: 'Playfair Display', Georgia, serif; font-weight: 600; font-size: 1.875rem; color: #fff; }
  @media (min-width: 768px) { .fc-head h2 { font-size: 2.25rem; } }
  .fc-desc { font-size: 1.25rem; margin-bottom: 32px; opacity: 0.9; max-width: 42rem; margin-left: auto; margin-right: auto; }
  .fc-btn-wrap { display: flex; flex-direction: column; gap: 16px; justify-content: center; align-items: center; }
  @media (min-width: 640px) { .fc-btn-wrap { flex-direction: row; } }
  .fc-btn {
    display: inline-flex; align-items: center; gap: 8px; padding: 16px 32px; border-radius: 8px;
    font-weight: 600; font-size: 1.125rem; text-decoration: none; transition: all 0.2s;
  }
  .fc-gold { background: var(--dental-gold); color: var(--dental-purple); }
  .fc-gold:hover { opacity: 0.9; }
  .fc-footnote { margin-top: 24px; font-size: 0.875rem; opacity: 0.75; }

  /* ══ Related ══ */
  .rel-section { padding: 48px 0 48px; background: hsl(var(--beige-hsl) / 0.2); }
  .rel-section .med-c { padding-left: 16px; padding-right: 16px; }
  .rel-title { font-family: 'Playfair Display', Georgia, serif; font-weight: 600; font-size: 1.25rem; color: var(--dental-purple); margin-bottom: 24px; }
  .rel-grid { display: grid; gap: 16px; }
  @media (min-width: 640px) { .rel-grid { grid-template-columns: repeat(3, minmax(0, 1fr)); } }
  .rel-card {
    display: flex; flex-direction: column; gap: 8px; background: #fff; padding: 20px; border-radius: 12px;
    border: 1px solid #e5e1d4; text-decoration: none; transition: all 0.2s;
  }
  .rel-card:hover { border-color: rgb(56 31 71 / 0.3); box-shadow: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1); }
  .rel-t { font-weight: 600; color: var(--dental-purple); font-size: 0.875rem; line-height: 1.375; transition: color 0.2s; }
  .rel-card:hover .rel-t { color: #4a2759; }
  .rel-d { color: var(--dental-gray); font-size: 0.75rem; line-height: 1.7; flex: 1; }
  .rel-more { display: flex; align-items: center; gap: 4px; color: var(--dental-gold); font-size: 0.75rem; font-weight: 500; margin-top: 4px; }

  /* ══ Stats Bar ══ */
  .stats-bar { background: rgb(56 31 71 / 0.05); border-top: 1px solid rgb(56 31 71 / 0.1); border-bottom: 1px solid rgb(56 31 71 / 0.1); }
  .stats-bar .container-c { padding-top: 24px; padding-bottom: 24px; }
  .stats-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 24px; }
  @media (min-width: 768px) { .stats-grid { grid-template-columns: repeat(4, minmax(0, 1fr)); gap: 32px; } }
  .stat { display: flex; align-items: center; gap: 12px; justify-content: center; }
  .stat-ic { flex-shrink: 0; width: 40px; height: 40px; border-radius: 999px; background: rgb(179 149 95 / 0.1); display: flex; align-items: center; justify-content: center; }
  .stat-v { font-size: 1.125rem; font-weight: 600; color: var(--dental-purple); line-height: 1.25; }
  .stat-l { font-size: 0.75rem; color: var(--dental-gray); line-height: 1.25; }
</style>
