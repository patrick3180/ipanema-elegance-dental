{
  "name": "carla-astro",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "node copy-assets.mjs && astro build",
    "test": "node --experimental-strip-types --test tests/**/*.test.ts",
    "preview": "astro preview"
  },
  "dependencies": {
    "@contentful/rich-text-html-renderer": "^17.0.0",
    "@contentful/rich-text-types": "^17.0.0",
    "astro": "^5.0.0"
  }
}
